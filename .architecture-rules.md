# Architecture Rules

1. All new functions must have type hints.
2. Every database call must be wrapped in try/except.
3. No raw SQL string concatenation or f-strings — use parameterized queries.
4. No bare `except:` clauses.
5. No secrets hardcoded in source files.